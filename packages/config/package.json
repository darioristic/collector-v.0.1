{
  "name": "config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"config package has no build step\"",
    "lint": "bunx eslint --config eslint.config.js ."
  }
}


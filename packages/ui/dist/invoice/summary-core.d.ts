export declare function SummaryCore({ includeVAT, includeTax, taxRate, currency, vatLabel, taxLabel, totalLabel, amountBeforeDiscount, discountTotal, subtotal, totalVat, total, }: {
    includeVAT?: boolean;
    includeTax?: boolean;
    taxRate?: number;
    currency?: string;
    vatLabel?: string;
    taxLabel?: string;
    totalLabel?: string;
    amountBeforeDiscount: number;
    discountTotal: number;
    subtotal: number;
    totalVat: number;
    total: number;
}): import("react/jsx-runtime").JSX.Element;

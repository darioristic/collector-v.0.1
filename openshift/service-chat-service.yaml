apiVersion: v1
kind: Service
metadata:
  name: chat-service
  labels:
    app: chat-service
spec:
  selector:
    app: chat-service
    deploymentconfig: chat-service
  ports:
    - name: http
      port: 4001
      targetPort: 4001
      protocol: TCP


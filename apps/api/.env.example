# Collector Dashboard API

# Primarna PostgreSQL konekcija (koristi se u produkciji/stagingu)
DATABASE_URL=postgres://collector:collector@localhost:5432/collector_dashboard

# Opcioni parametri za pool (korisno u cloud okruženju)
DB_MAX_CONNECTIONS=10
DB_SSL=false

# Fastify server konfiguracija
HOST=0.0.0.0
PORT=4000

# Kada DATABASE_URL nije postavljen, API će koristiti pg-mem in-memory bazu za razvoj/test

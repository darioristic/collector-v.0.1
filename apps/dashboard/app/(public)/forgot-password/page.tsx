import { redirect } from "next/navigation";

export default function ForgotPasswordRedirect() {
  redirect("/auth/forgot-password");
}

s default from Next? Wait autop? Note instructions require pure ASCII etc.
